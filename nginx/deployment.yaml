apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployement
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: my_nginx
        imagePullPolicy: Never
        ports:
        - containerPort: 80
        # - containerPort: 443
        # - containerPort: 22
        # resources:
        #   requests:
        #     cpu: "0.3"
        #     memory: "500Mi"
